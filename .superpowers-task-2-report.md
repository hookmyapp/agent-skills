# Plan 06 Task 2 — agent-skills billing reference

Repo: `agent-skills`, branch `ait-420-money-model-change` (verified, up to date with origin, clean tree before edits). Not pushed (controller pushes).

## Sweep

`grep -rn "plan\|2,000\|30,000\|Launch\|price\|\$12\|\$39" skills/integrate-hookmyapp/`

Hits and disposition:

- `skills/integrate-hookmyapp/references/billing.md` — the whole old-model surface (plans table, action/message wording, `starter`/`growth`/`pro` slugs, CLI upgrade picker text). Rewritten in full (see below).
- `skills/integrate-hookmyapp/SKILL.md:120` — billing row in the command table ("billing is pooled across your organization"). Wording still accurate for v2; not naming any old tier/price, left as is.
- `skills/integrate-hookmyapp/SKILL.md:63` — comment about "in-terminal plan changes" (CLI version-floor note, unrelated to pricing model). Left as is.
- `skills/integrate-hookmyapp/references/channels.md:42` — exit code 3 doc: "workspace has reached plan channel limit". Generic CLI-observed-behavior note, no old tier/price named. Money model v2 makes channels unlimited on both plans, which would make this stale, but the actual CLI validation change is owned by the CLI repo's Plan 05 task, not this billing-reference task — left untouched, flagged here for that task/repo to reconcile.
- `skills/integrate-hookmyapp/references/workspace.md:74` — exit code 2 doc: "billing / plan restriction", same category as above. Left untouched, same reasoning.
- `skills/integrate-hookmyapp/references/customers.md:10` — "no plan gate" (says customers surface is available on every plan, no tier gating). Still true under v2, no change needed.

No literal `$12`/`$39`/`2,000`/`30,000`/`Launch` matches existed anywhere in this repo.

## Changes to billing.md

- Added a `## Plans` section: Trial (free, 7 days, unlimited actions, no card) / Build ($1/mo, $10/yr, 200 actions) / Scale ($24/mo, $240/yr, 15,000 actions) / Above Scale (talk to us), unlimited channels and team included on both paid plans, plus the day-7 verdict rule and the action definition (customer words, no HTTP mechanics).
- `billing status`: replaced the stale `free`/`starter`/`growth`/`pro` (message-quota) plan list with `trial`/`build`/`scale` (action-quota); "messages used" -> "actions used"; noted trialing orgs report unlimited instead of a limit.
- `billing upgrade`: free-org picker now names `build`/`scale` instead of `starter`/`growth`/`pro`.
- Added `## Billing eligibility (REST)`: `GET /organizations/{orgId}/billing/eligibility` (org API key, per the existing api.md auth pattern) — `eligiblePlan`, `trialActions`, `trialStatus`. Framed strictly as informational (tell the human where they stand), never a gate for whether to send or retry.
- Added `## Paused-organization errors`: the two public codes `TRIAL_ENDED` and `PLAN_ENDED` with their exact safe messages, what they mean (every channel in the org paused), and the required agent reaction — surface the billing URL to the human, never retry through the pause. No internal reason strings or cohort/slug mechanics included.
- Extended `## Safety notes` with a line on the two pause codes.

## Version bump

`SKILL.md` `metadata.version`: `0.9.10` -> `0.9.11` (patch), plus the matching skill-version-marker echo command in the setup script block. Both updated together per repo convention (they're asserted to match).

## Commit

`AIT-420: billing reference, money model v2` (not pushed).

## Files touched

- `/Users/ordvir/COD/almighty-hookmyapp/.worktrees/money-model-change/agent-skills/skills/integrate-hookmyapp/references/billing.md`
- `/Users/ordvir/COD/almighty-hookmyapp/.worktrees/money-model-change/agent-skills/skills/integrate-hookmyapp/SKILL.md`

## Review fix (post-commit, amended)

Coordinator review flagged the no-em-dash rule: stripped all em dashes from
`billing.md` (the 5 newly-added spots plus 6 pre-existing ones in the file,
to satisfy the stated zero-hits check), reworded with commas/periods/colons,
no meaning changes. Commit title changed to
`AIT-420: billing reference, money model v2` via amend (still local, not
pushed). `grep -n "—" skills/integrate-hookmyapp/references/billing.md`
now returns zero hits.
